# Reference Inputs

Reference files in this directory are retained for project audit and formula /
terminology review. They are not package-distributed runtime data.

Current files:

- `zzz-data-introduction.txt` — NGA guide snapshot originally used by the team
  as a formula reference.

If a reference statement is promoted into formal data or formula behavior, the
implementation must cite the source anchor in code/tests/docs rather than
copying values without provenance.
