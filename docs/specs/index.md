# 哪些规格当前有效

本页是 current spec membership 的唯一事实源。如何新增、替换、撤回或评审 spec，见[规格如何定义、评审和演进](../rules/specs.md)。

## Current specs

Current specs: none

当前没有 registered current spec；不代表任意行为已获批准。需要 current behavior authority 的工作必须停止并请求 owner 决定，不得制造 placeholder spec 或选择 silent default。

| id  | version | path | scope summary |
| --- | ------- | ---- | ------------- |

完整 scope 只保存在对应 current spec 中。本 registry 不记录 owner、task、PR、commit、head、review status、progress、reminder 或其他进行中工作状态。
