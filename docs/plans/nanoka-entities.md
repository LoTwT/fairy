# Nanoka 后续实体实施计划

> [!IMPORTANT]
> 本文是 Nanoka 后续实体实施期间使用的临时计划，不是长期事实来源。全部实体完成后，应先将仍然有效的契约迁入对应正式规范，再删除本文及 `docs/index.md` 中的入口。

## 1. 目标

在不预先假定上游结构的前提下，为以下 Nanoka 实体逐步建立经过实际数据验证的规范、实现和测试：

- W-Engines（`weapon`）；
- Bangboos（`bangboo`）；
- Drive Discs（`equipment`）；
- Monsters（`monster`）；
- End Game 领域及 Shiyu（`shiyu`）、Boss（`boss`）、Simul（`simul`）子域。

实体范围和正式状态以 [Nanoka 数据源规范索引](../specs/nanoka/index.md) 为准。共享来源、HTTP、缓存、快照和发布契约以 [Nanoka 共享来源规范](../specs/nanoka/source.md) 为准。

## 2. 实施原则

- 先对全部后续实体做轻量调研，再确定实施顺序。
- 不创建未经上游结构验证的空实体规范。
- 每次只推进一个足够独立的实体或领域单元，完成“规范、实现、验证”闭环后再进入下一项。
- 实体特有规则保留在实体规范；只有被多个实体实际需要的机制才能提升为共享契约。
- 实际上游数据与设计不一致时，先修正规范，再使实现与规范一致。
- 本文只维护临时路线、检查清单、顺序和未决问题，不重复正式规范中的契约和实体状态。
- `docs/HISTORY.md` 是冻结的历史档案，不参与本计划，也不得用于记录实施过程或结果。

## 3. 总体阶段

### 阶段一：全实体轻量调研

对所有后续实体进行低成本结构探测，记录：

- 摘要和详情端点是否存在；
- JSON 顶层结构和最小样本；
- ID 的发现方式和排序要求；
- 支持的语言；
- 摘要与详情是一对一、一对多还是其他关系；
- 实体之间是否存在引用；
- 不同版本是否存在资源或结构差异；
- 与现有 Agents 抓取模型的主要差异；
- 对 fetch manifest、快照布局和离线验证的潜在影响。

完成条件：

- 每个实体或子域至少检查一个有效版本；
- 已记录资源发现方式和语言覆盖；
- 已识别明显的跨实体依赖；
- 已列出可能影响共享基础设施的差异；
- 信息足以确定普通实体的建议实施顺序；
- 未将未经确认的结构写成正式契约。

### 阶段二：共享基础设施影响评估

根据轻量调研结果确定：

- 抓取命令一次处理一个实体还是整个版本的全部实体；
- 多实体是否共用一份 `fetch-manifest.json`；
- manifest 如何表达实体类型及各实体计数；
- 多实体快照的原子发布和失败保护边界；
- 现有 `nanoka-fetch-manifest/v1` 是否足够；
- 目录、缓存、并发、进度和离线验证是否需要共享扩展；
- 如何保持现有 Agents 快照和验证行为兼容。

完成条件：

- 跨实体机制有明确归属；
- 必要的共享契约变更已写入 `source.md`；
- 实体特有差异没有被过早泛化；
- 已确认首个实体可以在稳定边界上实施。

### 阶段三：确定实施顺序

优先级依据：

1. 被其他实体引用的基础实体；
2. 结构清楚且能验证共享设计的实体；
3. 普通独立实体；
4. 关系复杂或版本差异明显的实体；
5. End Game 领域和子域。

当前展示顺序不是最终承诺。完成轻量调研后，在本文记录确认后的队列和排序依据。

### 阶段四：普通实体逐个闭环

对 W-Engines、Bangboos、Drive Discs 和 Monsters 按确认后的顺序逐个实施。每个实体都必须执行第 4 节的标准流程，前一个实体完成闭环后再进入下一个实体。

### 阶段五：End Game 领域调研与建模

在创建 End Game 正式规范前确认：

- Shiyu、Boss 和 Simul 是否共享赛季、阶段、关卡或其他 ID；
- Boss 是否引用 Monsters 或其他普通实体；
- 是否存在版本相关的可选资源；
- 三个子域能否独立抓取、发布和验证；
- 是否必须整组抓取才能满足一致性要求；
- 应使用一份领域规范，还是领域规范加子域规范。

完成条件：

- 领域边界和子域关系已由实际数据支持；
- 正式规范的拆分方式已确定；
- 子域实施顺序及整体一致性要求已明确。

### 阶段六：End Game 子域逐个闭环

按领域调研确定的依赖顺序逐个实施子域，并在全部子域完成后增加 End Game 整体一致性验证。

### 阶段七：全实体综合验证与计划清理

- 对完整版本快照运行在线抓取和离线验证；
- 确认所有实体的资源覆盖、计数和引用关系；
- 确认失败保护、缓存复用、原子发布和版本锁在完整范围内成立；
- 将长期有效规则迁移到 `source.md` 或对应实体规范；
- 在规范索引中更新最终状态；
- 确认本文不再包含唯一信息；
- 删除本文；
- 删除 `docs/index.md` 中的临时计划入口。

## 4. 单实体标准流程

每个实体按以下顺序完成：

1. **调研上游**：获取真实样本，确认端点、资源关系、语言、ID 和版本差异。
2. **编写规范**：定义目标、非目标、端点、发现规则、最低结构、一致性、布局、manifest 扩展、测试和验收标准。
3. **评审共享影响**：确认是否可以复用现有机制，是否需要共享契约变更或 manifest schema 升级。
4. **实现抓取**：完成资源发现、最低结构校验、路径策略、原始字节保存、清单记录和进度事件。
5. **实现离线验证**：校验文件、哈希、计数、资源覆盖及实体特有关系。
6. **补齐自动化测试**：使用 mock fetch、临时目录和最小 fixture 覆盖成功及失败路径。
7. **执行在线抓取**：选择真实版本，检查目录、资源数量、原始 JSON、清单和终端进度。
8. **验证缓存与失败保护**：重复抓取以确认条件请求或缓存复用，并确认失败不会替换完整快照。
9. **执行离线验收**：运行 verify，并在测试副本中验证缺失、篡改和关系错误能够被发现。
10. **修正规范与实现**：实际数据暴露差异时同步修正，保持规范和实现一致。
11. **更新正式状态**：将实体规范和规范索引更新为经实际验证的状态。
12. **完成独立变更单元**：完成适合独立评审和回滚的提交或 PR，再进入下一实体。

## 5. 单实体调研检查表

- [ ] 摘要端点已确认；
- [ ] 详情端点或其他资源端点已确认；
- [ ] JSON 顶层类型和最低可依赖结构已确认；
- [ ] ID 来源、合法格式和稳定排序规则已确认；
- [ ] 语言范围和回退行为已确认；
- [ ] 摘要与详情的数量及对应关系已确认；
- [ ] 可选、缺失或版本限定资源已确认；
- [ ] 跨实体引用及引用缺失行为已确认；
- [ ] 本地文件布局已确认；
- [ ] asset kind 和稳定 asset ID 已确认；
- [ ] summary 计数字段已确认；
- [ ] 漂移检测和拒绝条件已确认；
- [ ] 自动化测试矩阵已定义；
- [ ] 实际在线抓取已完成；
- [ ] 离线 verify 已通过；
- [ ] 正式规范和索引状态已更新。

## 6. 实施队列

轻量调研完成前暂不锁定顺序。初始候选范围为：

```text
W-Engines
Bangboos
Drive Discs
Monsters
End Game 领域
End Game 子域
```

调研完成后在此处替换为确认后的队列，并简要记录排序依据。实体的正式完成状态仍只维护在规范索引中。

## 7. 未决问题

轻量调研期间至少需要解决：

- 不同实体是否都存在可用的摘要索引；
- 是否所有详情资源都覆盖 `zh` 和 `en`；
- 多实体加入同一版本目录后，快照发布单位是什么；
- 一份 manifest 是否能完整表达全部实体而不改变现有字段语义；
- 实体抓取能否独立重跑，还是必须整版本重建；
- End Game 子域之间及其与 Monsters 的引用边界是什么。

问题一旦形成长期契约，应迁入正式规范并从本节删除。

## 8. 删除条件

只有同时满足以下条件时才能删除本文：

- 索引中的所有计划实体均已实现并验证，或已通过正式决策移出范围；
- 所有长期有效的共享规则已进入 `source.md`；
- 所有实体特有规则已进入对应实体规范；
- 正式状态已更新到规范索引；
- 未决问题均已解决、迁移或明确取消；
- `docs/index.md` 中的临时入口将与本文一并删除。
