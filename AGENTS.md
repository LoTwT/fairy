# Fairy 智能体工作规则

## 渐进式披露

- 本文件只保存全仓通用且长期有效的工作规则，不维护文档目录。
- [docs/index.md](docs/index.md) 是唯一的文档入口与路由来源；从索引按需读取与当前任务有关的文档。
- 每个事项只保留一个唯一权威来源。入口只提供链接，不复制正文；详细内容写入独立文档。
- 只有出现真实内容时才新增文档或目录，不预建空结构，也不把任务、负责人或临时状态写进文档。

## 变更与评审

- 每个拉取请求（PR）只处理一个事项，并保持变更内容便于审查。
- 合并前需要维护者明确评审与批准；拉取请求描述必须说明精确范围、验证结果和未改范围。
- 已发布的 `@randomplay/core`、`@randomplay/data`、`@randomplay/cli` 软件包版本不可删除、覆盖或重新发布。

## 验证

提交评审前至少运行：

- `pnpm check`
- `git diff --check`
- 检查现存 Markdown 文件的相对链接

修改依赖或包管理器时，还必须运行 `pnpm install --frozen-lockfile`，并在锁文件更新后重新运行上述检查项。
