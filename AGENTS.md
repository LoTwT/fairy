# Agent 工作指南

项目文档主要使用中文，必要时使用英文。

- **渐进式披露**：需要了解项目文档时，以 `docs/index.md` 为统一入口，仅按当前任务需要查阅相关内容。
- **单一事实来源**：同一项知识应由一个明确的权威来源维护，其他位置通过引用使用，避免重复定义。
- **指令作用域与优先级**：遵循当前修改范围内最具体的适用规则，无法消解的指令冲突应在执行前明确指出。
- **先理解，后修改**：修改前先理解相关代码、测试、配置和既有约定，不根据名称或表象猜测行为。
- **语义明确**：代码应通过准确的命名、类型、接口和结构直接表达业务意图；准确性优先于简短，允许使用更长的函数名、类名和变量名，避免含糊缩写、泛化命名及依赖注释补充核心语义。
- **最小充分变更**：实施能够完整满足要求的最小连贯变更，不进行无关的重构、清理或格式化。
- **基于证据的验证与报告**：通过实际检查验证结果，只报告已确认的事实，并明确说明失败、跳过和未验证事项。
- **数据管理状态同步**：凡涉及已接入实体的增删、整合规则/来源版本/`integrated` 制品的修改，或合并 PR、拉取、切换分支使 `integrated` 与已有本机记录变化，任务完成前必须一并检查并维护受影响工作区的数据管理状态，唯一流程见[来源数据整合规范](docs/specs/data/integration.md#数据管理状态的同步维护)。
  - 已受管理工作区：按上述流程同步数据、管理记录与报告，完成持锁验证与消费准备后才能报告完成。
  - 独立 worktree 中的变更：既验证该工作区，也在变更同步回主工作区后检查主工作区已有的管理状态。
  - 没有控制目录的新克隆或静态工作区：保持静态消费能力，不为普通构建、测试强制创建管理记录。
  - 不涉及数据快照或管理契约的改动：无需无条件重写本机状态。
  - `BUSY`、未完成事务或无法确认归属的现场：保留现场并明确报告；不能用干净克隆中的 CI 成功代替主工作区的维护验收。
  - 控制目录、锁、raw 与一次性备份保持本机属性，不加入 Git 或 npm 包。
- **历史档案只读**：`docs/HISTORY.md` 是既有发布历史结果的冻结档案，不得修改或追加。当前及未来工作的计划、进度、决策和结果应维护在对应规范、临时计划或 Git 记录中。若发现历史事实错误，必须先取得用户明确授权才能更正。
