{
  "name": "@fairy/data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "!src/**/*.test.ts",
    "dist",
    "cleaned",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "sync-cleaned": "node scripts/sync-cleaned.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@fairy/core": "workspace:*"
  }
}
