{
  "name": "fairy",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write . '!packages/data/integrated/**'",
    "format:check": "oxfmt --check .",
    "check:spec-types": "pnpm --filter @randomplay/core build && pnpm --filter @randomplay/core exec tsc --ignoreConfig --noEmit --allowImportingTsExtensions --strict --exactOptionalPropertyTypes --noUncheckedIndexedAccess --target ES2022 --module ESNext --moduleResolution Bundler ../../docs/specs/effects/contract-examples.ts",
    "check": "pnpm check:packages && pnpm lint && pnpm format:check && pnpm check:spec-types && pnpm --recursive --if-present check",
    "prepare": "simple-git-hooks",
    "check:packages": "node scripts/check-packages.ts"
  },
  "devDependencies": {
    "lint-staged": "^17.2.0",
    "oxfmt": "^0.60.0",
    "oxlint": "^1.75.0",
    "simple-git-hooks": "^2.13.1"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm lint-staged"
  },
  "lint-staged": {
    "*.{js,cjs,mjs,jsx,ts,tsx}": [
      "oxlint",
      "oxfmt --check"
    ],
    "*.{json,jsonc,md,yaml,yml,css,scss,html}": "oxfmt --check"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.13.1"
}
