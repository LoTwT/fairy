{
  "name": "fairy-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "type": "module",
  "scripts": {
    "check": "pnpm -r --if-present check",
    "test": "pnpm -r --if-present test"
  }
}
