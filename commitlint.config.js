// This repo is `type: module`, so this config uses ESM syntax.
// If the repo ever moves back to CommonJS, rename this file to
// `commitlint.config.cjs` and use `module.exports = ...`.
export default {
  extends: ["@commitlint/config-conventional"],
}
